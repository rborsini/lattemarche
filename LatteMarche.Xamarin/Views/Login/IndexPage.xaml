<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LatteMarche.Xamarin.Views.Login.IndexPage">
    <ContentPage.Content>
        <StackLayout>
            <!--<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />-->
            <Label Text="Username" FontSize="Medium" />
            <Entry Text="{Binding Username}" d:Text="Username" FontSize="Small" />
            <Label Text="Quantità" FontSize="Medium" />
            <Editor Text="{Binding Password}" d:Text="Password" FontSize="Small" Margin="0" />
            <Button Margin="0,10,0,0" Text="Accedi" Command="{Binding LoginCommand}"  />            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>